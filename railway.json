{
  "build": {
    "builder": "NIXPACKS"
  },
  "startCommand": "php artisan storage:link && php artisan migrate --force && php artisan db:seed --force && php artisan serve --host=0.0.0.0 --port=$PORT"
}
